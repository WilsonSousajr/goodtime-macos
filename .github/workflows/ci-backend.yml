# Stub — full implementation tracked in issue #67.
# Will spin up an ephemeral Postgres, apply every SQL migration in order,
# and lint docker-compose.yml.
name: CI · Backend
on:
  pull_request:
    paths:
      - backend/**
  push:
    branches: [master]
    paths:
      - backend/**

jobs:
  validate-migrations:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: TODO — apply migrations to ephemeral Postgres
        run: echo "see issue #67"
